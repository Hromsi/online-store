export interface IProduct {
    id: string;
    brand: string;
    stock: number;
    description: string;
    images: string[];
    thumbnail: string;
    title: string;
    price: number;
};